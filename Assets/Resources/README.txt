Any assets that need to be loaded dynamically at runtime:

- These will increase the build size so use sparingly
- C# Command: Resources.Load(): http://docs.unity3d.com/ScriptReference/Resources.Load.html